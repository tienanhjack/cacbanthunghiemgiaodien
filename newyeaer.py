from calendar import*
year = int(input('Enter Year:'))
print(calendar(year, 2, 2, 8, 3))

# 2=2 character for days (Mo, tu, etc)
# 1=1 line (row) for each week
# 8=8 rows for each month
# 3=3 columns for all months of the year.
# clcoding.com